<!DOCTYPE html>
<html>
<head>
  <title>Inforad <% if (host) { %><%= host %><% } %></title>
  <style type="text/css">
    body {
      margin: 0;
      background: #000;
    }
    iframe {
      border: 0;
      position: absolute;
      width: 1080px;
      height: 1920px;
      overflow: hidden;
      -webkit-transition: all 300ms linear;
    }
    .changing {
      opacity: 0;
    }
  </style>
</head>
<body>
  <iframe src="/info/clock" id="frame" scrolling="no"></iframe>

  <script type="text/javascript">
  <% if (host) { %>var host = '<%= host %>';<% } %>
  <% if (host) { %>var DEFAULT = <%- defaultScene ? '"' + defaultScene + '"' : false %>;<% } %>
  </script>
  <script src="/socket.io/socket.io.js"></script>
  <script src="/javascript/app.min.js"></script>
</body>
</html>